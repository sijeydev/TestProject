name: Build Kivy APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.8'

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Удаляем глобальный NDK
      run: |
        sudo rm -rf /usr/local/lib/android/sdk/ndk
        sudo rm -rf /usr/local/lib/android/sdk/ndk-bundle

    - name: Ставим p4a и УБИВАЕМ pyjnius
      run: |
        pip install --user --upgrade pip
        pip install --user cython virtualenv
        pip install --user python-for-android
        
        # Находим где лежат рецепты
        P4A_DIR=$(python -c "import pythonforandroid.recipes; print(pythonforandroid.recipes.__path__[0])")
        echo "P4A recipes at: $P4A_DIR"
        
        # УДАЛЯЕМ РЕЦЕПТ ФИЗИЧЕСКИ
        rm -rf $P4A_DIR/pyjnius
        echo "✅ Рецепт pyjnius удалён!"
        
        # УДАЛЯЕМ ИЗ КЭША
        rm -rf ~/.local/share/python-for-android/packages/pyjnius
        rm -rf ~/.local/share/python-for-android/build/other_builds/pyjnius*
        
        # ПРОВЕРЯЕМ
        echo "🔍 Проверка: остались ли рецепты pyjnius?"
        find ~/.local/share/python-for-android -name "*pyjnius*" -type d 2>/dev/null || echo "✅ Чисто, pyjnius не найден"

    - name: Качаем SDK и NDK
      run: |
        mkdir -p ~/android-sdk
        cd ~/android-sdk
        
        wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
        unzip -q commandlinetools-linux-11076708_latest.zip
        mkdir -p cmdline-tools/latest
        mv cmdline-tools/* cmdline-tools/latest/ 2>/dev/null || true
        
        wget https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
        unzip -q android-ndk-r25b-linux.zip

    - name: Принимаем лицензии
      run: |
        cd ~/android-sdk
        yes | ./cmdline-tools/latest/bin/sdkmanager --licenses --sdk_root=$HOME/android-sdk

    - name: Ставим платформы
      run: |
        cd ~/android-sdk
        ./cmdline-tools/latest/bin/sdkmanager "platforms;android-33" "build-tools;33.0.2" --sdk_root=$HOME/android-sdk

    - name: Сборка APK (Kivy 2.2.1)
      env:
        ANDROIDSDK: /home/runner/android-sdk
        ANDROIDNDK: /home/runner/android-sdk/android-ndk-r25b
      run: |
        # ФИНАЛЬНАЯ ЗАЧИСТКА
        rm -rf ~/.local/share/python-for-android
        
        p4a apk --private . \
          --package=org.example.myapp \
          --name "My App" \
          --version 0.1 \
          --bootstrap=sdl2 \
          --requirements=python3,kivy==2.2.1 \
          --arch=arm64-v8a \
          --release \
          --ndk-dir=$ANDROIDNDK \
          --sdk-dir=$ANDROIDSDK \
          --android-api=33 \
          --minsdk=21

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: app-apk
        path: bin/*.apk
